/* // Uncomment the imports below if you plan on using them
const LinkedList = require("../../Starter/linkedList");
const Node = require("../../Starter/node");
*/

/*
Complete the following Linked List remove duplicates problem below. There is only 1 input, which is the Linked List. The goal is to find any duplicate nodes and remove them. In essence, all nodes should be unique. Return Linked List once that is done.

function LinkedList() {
    this.head = null;
}

function Node(data, next = null) {
    this.data = data;
    this.next = next;
}
*/


const removeDuplicates = linkedList => {
  
}

module.exports = removeDuplicates;

/*

Linked List = { 3 => { 3 => { 3 => { 4 => { 5 => { 5 => null } } } }
Expected Output: { 3 => { 4 => { 5 => null } } }

*/