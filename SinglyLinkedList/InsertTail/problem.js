// ? In Visual Studo Code, install "Better Comments" to see colorful effects of comments

/* // Uncomment the imports below if you plan on using them
* const LinkedList = require("../../Starter/linkedList");
* const Node = require("../../Starter/node");
*/

/*
TODO: Complete the following Linked List insert tail problem below. There are 2 inputs, the Linked List and the node in which you want to insert to the end of that Linked List. Once the node is inserted to the tail, return the Linked List.

function LinkedList() {
    this.head = null;
} 

function Node(data, next = null) {
    this.data = data;
    this.next = next;
}
*/

const insertTail = (linkedList, node) => {

}

module.exports = insertTail;

/*
! Test Case: 
Linked List = { 3 => { 4 => { 5 => null } } }
Input: { 6 => null }
Expected Output: { 3 => { 4 => { 5 => { 6 => null } } } }
*/
