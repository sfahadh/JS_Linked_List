// ? In Visual Studo Code, install "Better Comments" to see colorful effects of comments

/* // Uncomment the imports below if you plan on using them
* const LinkedList = require("../../Starter/linkedList");
* const Node = require("../../Starter/node");
*/

/*
TODO: Complete the following Linked List remove mid problem below. There is only 1 input, which is the linkedlist. Remove the middle node and once that's done, return the Linked List.

function LinkedList() {
    this.head = null;
}

function Node(data, next = null) {
    this.data = data;
    this.next = next;
}
*/ 


const removeMid = linkedList => {
    
}

module.exports = removeMid;

/*
! Test Case 1:
Linked List = { 3 => { 4 => { 5 => null } } }
Expected Output: { 3 => { 5 => null } } 

! Test Case 2:
Linked List = { 4 => { 5 => { 6 => { 7 => null } } } }
Expected Output: { 4 => { 5 => { 7 => null } } } 
*/

/*
    ? You can assume that the linked list will always have at LEAST 3 nodes.
*/